{
  "name": "agentflow",
  "version": "1.0.0",
  "description": "QA automation portfolio — insurance upload flow",
  "scripts": {
    "server": "ts-node src/server/index.ts",
    "generate:fixtures": "ts-node scripts/generateFixtures.ts",
    "generate:dashboard": "ts-node scripts/generateStaticDashboard.ts",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.0.0",
    "allure-playwright": "^3.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5",
    "wait-on": "^7.2.0"
  },
  "ts-node": {
    "compilerOptions": {
      "module": "commonjs"
    }
  }
}
